@model ViewModel

<div class="d-flex justify-content-between">
    
    <ul class="d-flex list-unstyled">
        <li>
            <a href="/dashboard/{id}" class="@(Model.CurrentView == "Dashboard" ? "disabled" : "")">Home</a> | 
        </li>
        <li>
            <a href="/weddings/new" class="@(Model.CurrentView == "NewWedding" ? "disabled" : "")">Plan a Wedding</a> | 
        </li>
        <li>
            <a asp-controller="WeddingPlanner" asp-action="Logout">Logout</a>
        </li>
    </ul>
    
</div>